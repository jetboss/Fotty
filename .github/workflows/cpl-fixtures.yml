name: CPL Fixture Monitor

on:
  schedule:
    - cron: "47 10 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: cpl-fixture-monitor
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "24"
      - name: Validate and refresh the reviewed CPL manifest
        run: node tools/audit-cpl-fixtures.mjs --update
      - name: Open or update a fixture review pull request
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if git diff --quiet -- web/public/data/cpl-2026-fixtures.json; then
            echo "CPL schedule is current."
            exit 0
          fi

          branch="automation/cpl-fixtures"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git switch -C "$branch"
          git add web/public/data/cpl-2026-fixtures.json
          git commit -m "chore: refresh CPL fixtures"
          git push --force-with-lease origin "HEAD:refs/heads/$branch"

          existing="$(gh pr list --head "$branch" --state open --json number --jq '.[0].number // empty')"
          if [[ -z "$existing" ]]; then
            gh pr create \
              --base main \
              --head "$branch" \
              --title "chore: review current CPL fixture changes" \
              --body "The daily fail-closed CPL monitor found a verified change in match identity or UTC kickoff. The generated manifest passed the complete 39-match validation. Review the diff before merging; a partial or structurally changed upstream feed is rejected."
          else
            echo "Updated existing CPL fixture PR #$existing."
          fi
